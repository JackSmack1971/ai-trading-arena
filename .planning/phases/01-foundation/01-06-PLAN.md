---
phase: 01-foundation
plan: 06
type: execute
wave: 5
gap_closure: true
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - packages/broker-paper/src/__tests__/risk-gate.test.ts
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "PaperRiskGate boundary-value tests exist for MAX_DRAWDOWN, NO_NEGATIVE_CASH, MAX_SYMBOL_EXPOSURE, and MAX_TOTAL_EXPOSURE"
    - "Each tested rule has exactly one passing case (at-threshold or below) and one failing case (just over threshold)"
    - "All 4 failing cases assert ruleId on the rejection event"
    - "pnpm --filter @arena/broker-paper test passes with the new test file"
    - "pnpm typecheck passes across all packages"
  artifacts:
    - path: "packages/broker-paper/src/__tests__/risk-gate.test.ts"
      provides: "Boundary-value tests for the 4 untested PaperRiskGate rules (gap closure for SC-2)"
  key_links:
    - from: "packages/broker-paper/src/__tests__/risk-gate.test.ts"
      to: "packages/broker-paper/src/risk-gate.ts"
      via: "import PaperRiskGate, RiskGateContext"
      pattern: "direct PaperRiskGate.evaluateOrder() calls with controlled context"

---

<objective>
Close the SC-2 gap from Phase 1 VERIFICATION.md: add boundary-value Vitest tests for the four
PaperRiskGate rules that have no test coverage — MAX_DRAWDOWN, NO_NEGATIVE_CASH,
MAX_SYMBOL_EXPOSURE, and MAX_TOTAL_EXPOSURE.

Each rule gets two assertions: one case that passes at the configured threshold, and one that
rejects when the value exceeds the threshold by one decimal unit. The failing cases assert
`result.event.ruleId` to prove the correct rule fired.

These tests exercise PaperRiskGate directly (not through PaperBroker) so the context can be
constructed with exact boundary values without triggering earlier rules in the evaluation chain.
</objective>

<execution_context>
@F:/ai-trading-arena/.claude/get-shit-done/workflows/execute-plan.md
@F:/ai-trading-arena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/ai-trading-arena/.planning/phases/01-foundation/01-VERIFICATION.md
@F:/ai-trading-arena/.planning/phases/01-foundation/01-CONTEXT.md
@F:/ai-trading-arena/packages/broker-paper/src/risk-gate.ts
@F:/ai-trading-arena/packages/broker-paper/src/__tests__/broker.test.ts
@F:/ai-trading-arena/packages/core/src/schemas/broker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/broker-paper/src/__tests__/risk-gate.test.ts with boundary-value tests for 4 untested rules</name>
  <read_first>
    - packages/broker-paper/src/risk-gate.ts — PaperRiskGate.evaluateOrder() evaluation chain, exact comparison operators (gt vs gte/lt vs lte), RiskGateContext interface shape
    - packages/broker-paper/src/__tests__/broker.test.ts — existing test conventions: import style, T1 timestamp constant, LENIENT_RISK_CONFIG shape
    - packages/core/src/schemas/broker.ts — PortfolioSummary field names (agentId, cashBalance, equity, totalPositionValue, unrealizedPnl, realizedPnl, exposurePct, currentDrawdownPct, snapshotAt)
    - packages/core/src/schemas/risk.ts — RiskConfig field names for constructing per-rule configs
  </read_first>
  <action>
Create packages/broker-paper/src/__tests__/risk-gate.test.ts as a new file.

The file tests PaperRiskGate directly — not through PaperBroker. This gives exact control over
RiskGateContext values without triggering earlier rules in the evaluation chain.

Evaluation chain order in evaluateOrder() (rules 1–9):
  1. NO_REAL_EXECUTION
  2. NO_LEVERAGE
  3. MAX_DRAWDOWN ← NEW
  4. MAX_ORDERS_PER_MINUTE
  5. MAX_STRATEGY_SWITCHES_PER_HOUR
  6. NO_NEGATIVE_CASH ← NEW
  7. MAX_POSITION_SIZE
  8. MAX_SYMBOL_EXPOSURE ← NEW
  9. MAX_TOTAL_EXPOSURE ← NEW

Rules tested here (3 and 6–9): all rely on values supplied in RiskGateContext, so setting earlier
context fields to "safe" values lets each test isolate its target rule.

**LENIENT_RISK_CONFIG** (copy from broker.test.ts conventions, set all limits to high values so only
the target rule can fire):
```
maxPositionPct: '100'
maxSymbolExposurePct: '100'
maxTotalExposurePct: '100'
maxDrawdownPct: '100'
maxOrdersPerMinute: 100
maxStrategySwitchesPerHour: 100
allowNegativeCash: false
allowLeverage: false
allowRealExecution: false
```

**makeCtx helper** — builds a baseline RiskGateContext where no rule fires:
- runId: 'run-gate', agentId: 'agent-a', timestamp: T1
- request: { symbol: 'BTC-USD', side: 'BUY', quantityUsd: '100.00', orderType: 'MARKET' }
- portfolio: { agentId: 'agent-a', cashBalance: '10000.00', equity: '10000.00',
               totalPositionValue: '0', unrealizedPnl: '0', realizedPnl: '0',
               exposurePct: '0', currentDrawdownPct: '0', snapshotAt: T1 }
- availableCash: '10000.00'
- positions: []
- ordersThisMinute: 0
- strategySwitchesThisHour: 0

**Test 1 — MAX_DRAWDOWN**
Config: { ...LENIENT_RISK_CONFIG, maxDrawdownPct: '10' }
Rule fires when: currentDrawdownPct.gt(maxDrawdownPct)
  Case A — just below threshold (PASSED):
    portfolio.currentDrawdownPct = '9.99' → result.decision === 'PASSED'
  Case B — one unit over threshold (REJECTED):
    portfolio.currentDrawdownPct = '10.01' → result.decision === 'REJECTED'
    result.event.ruleId === 'MAX_DRAWDOWN'

**Test 2 — NO_NEGATIVE_CASH**
Config: { ...LENIENT_RISK_CONFIG, allowNegativeCash: false }
Rule fires when: side=BUY AND !allowNegativeCash AND availableCash.lt(requestedNotional)
Context values that keep earlier rules silent:
  equity = '2000.00', cashBalance = '1000.00', totalPositionValue = '1000.00'
  (so position pct = 1000/2000 = 50% ≤ 100% maxPositionPct — MAX_POSITION_SIZE won't fire)
  ordersThisMinute = 0, strategySwitchesThisHour = 0, currentDrawdownPct = '0'
  Case A — exactly at available cash (PASSED, because lt not lte):
    availableCash = '1000.00', request.quantityUsd = '1000.00' → result.decision === 'PASSED'
  Case B — one cent over available cash (REJECTED):
    availableCash = '1000.00', request.quantityUsd = '1000.01' → result.decision === 'REJECTED'
    result.event.ruleId === 'NO_NEGATIVE_CASH'
    Note: MAX_POSITION_SIZE (rule 7) fires AFTER NO_NEGATIVE_CASH (rule 6), so it does not interfere.
    With maxPositionPct=100, MAX_POSITION_SIZE anyway passes: 1000.01/2000*100 = 50.005% ≤ 100%.

**Test 3 — MAX_SYMBOL_EXPOSURE**
Config: { ...LENIENT_RISK_CONFIG, maxSymbolExposurePct: '50' }
Rule fires when: projectedSymbolExposurePct.gt(maxSymbolExposurePct)
  projectedSymbolExposure = (existing BTC-USD position value) + requestedNotional (for BUY)
  projectedSymbolExposurePct = projectedSymbolExposure / equity * 100
  Use no existing position, equity = '1000.00', availableCash = '1000.00'
  Keep MAX_POSITION_SIZE silent: maxPositionPct = '100' → 500/1000*100 = 50% ≤ 100% → PASS
  Case A — exactly at 50% (PASSED, because gt not gte):
    request.quantityUsd = '500.00' → projectedSymbolPct = 50.0% → result.decision === 'PASSED'
  Case B — one cent over threshold (REJECTED):
    request.quantityUsd = '500.01' → projectedSymbolPct > 50% → result.decision === 'REJECTED'
    result.event.ruleId === 'MAX_SYMBOL_EXPOSURE'

**Test 4 — MAX_TOTAL_EXPOSURE**
Config: { ...LENIENT_RISK_CONFIG, maxTotalExposurePct: '50' }
Rule fires when: projectedTotalExposurePct.gt(maxTotalExposurePct)
  projectedTotalExposure = totalPositionValue + requestedNotional (for BUY)
  projectedTotalExposurePct = projectedTotalExposure / equity * 100
  Use no existing positions, equity = '1000.00', totalPositionValue = '0', availableCash = '1000.00'
  Keep MAX_SYMBOL_EXPOSURE silent: maxSymbolExposurePct = '100' → 500/1000*100 = 50% ≤ 100% → PASS
  Case A — exactly at 50% (PASSED):
    request.quantityUsd = '500.00' → projectedTotalPct = 50.0% → result.decision === 'PASSED'
  Case B — one cent over threshold (REJECTED):
    request.quantityUsd = '500.01' → projectedTotalPct > 50% → result.decision === 'REJECTED'
    result.event.ruleId === 'MAX_TOTAL_EXPOSURE'

Do NOT include fenced code blocks or a full implementation in this action — write the file directly
using the Write tool. Use the patterns above as the implementation spec.
  </action>
  <acceptance_criteria>
    - packages/broker-paper/src/__tests__/risk-gate.test.ts exists
    - File contains 4 describe or it blocks covering MAX_DRAWDOWN, NO_NEGATIVE_CASH, MAX_SYMBOL_EXPOSURE, MAX_TOTAL_EXPOSURE
    - Each rule has exactly one PASSED case and one REJECTED case asserting ruleId
    - `pnpm --filter @arena/broker-paper test` exits 0 (all tests pass, including existing broker.test.ts)
    - No TypeScript errors in the new file when running `pnpm --filter @arena/broker-paper typecheck`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the full test suite to confirm no regressions</name>
  <read_first>
    - packages/broker-paper/src/__tests__/risk-gate.test.ts (just written — verify it exists)
  </read_first>
  <action>
Run from workspace root (F:\ai-trading-arena):

1. pnpm --filter @arena/broker-paper test
   Expected: all tests pass including the 4 new boundary-value tests, 0 failures
2. pnpm typecheck
   Expected: exits 0 across all 11 packages, zero TypeScript errors
3. pnpm test
   Expected: all packages pass (core, db, telemetry, broker-paper, feeds, strategies, worker)
  </action>
  <acceptance_criteria>
    - `pnpm --filter @arena/broker-paper test` exits 0
    - `pnpm typecheck` exits 0 with no errors
    - `pnpm test` exits 0 across all packages
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file | Pure in-process Vitest tests — no network calls, no file I/O, no external dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | npm/pip installs | accept | No new packages — test uses only existing broker-paper and core types |
</threat_model>

<verification>
Gap closure gate — all must pass before updating VERIFICATION.md status to passed:

```bash
# 1. New risk gate tests pass
pnpm --filter @arena/broker-paper test
# Expected: exits 0, output includes 4 new tests for MAX_DRAWDOWN/NO_NEGATIVE_CASH/MAX_SYMBOL_EXPOSURE/MAX_TOTAL_EXPOSURE

# 2. No TypeScript regressions
pnpm typecheck
# Expected: exits 0

# 3. Full test suite still green
pnpm test
# Expected: exits 0 across all packages
```
</verification>

<success_criteria>
- packages/broker-paper/src/__tests__/risk-gate.test.ts exists with 4 boundary-value test pairs
- Each of the 4 rules has a PASSED case (at threshold) and a REJECTED case (just over) with ruleId assertion
- pnpm --filter @arena/broker-paper test exits 0
- pnpm typecheck exits 0
- pnpm test exits 0 across all packages
- SC-2 gap closure: VERIFICATION.md can be updated to status: passed
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done.
</output>
